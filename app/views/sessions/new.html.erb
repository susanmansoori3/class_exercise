<h3> <%= @user_created_message %> </h3>


<h1>Log in</h1>

<!-- Because your @user will post back to user, we need
to write code to change that. It's a form FOR a user so
it looks for the User -->

<%= form_for @user, :url => { :controller => "sessions", :action => "create" } do |f| %>
  <% flash.each do |name, msg| -%>
  <%= content_tag :div, msg, class: name %>  
  <% end %>
    <%= f.text_field :email %><br />
    <%= f.password_field :password %><br />
    <%= f.submit "Login" %>
<% end %>

<%= link_to 'New user? Create an account here', new_user_path %>
